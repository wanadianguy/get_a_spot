import 'package:flutter/material.dart';

class ChangeMdpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Changer le Mot de Passe'),
      ),
      body: Center(
        child: Text('Page pour changer le mot de passe'),
      ),
    );
  }
}
