import 'package:flutter/material.dart';

class CampusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Campus'),
      ),
      body: Center(
        child: Text('Informations sur le Campus'),
      ),
    );
  }
}
